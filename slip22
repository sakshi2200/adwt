q1


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>3D Text Effects</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #333;
        }

        .container {
            text-align: center;
            max-width: 80%;
            margin: 0 auto;
        }

        h1 {
            font-size: 80px;
            text-transform: uppercase;
            font-weight: bold;
            color: #333;
            text-shadow: 4px 4px 10px rgba(0, 0, 0, 0.3), -4px -4px 10px rgba(0, 0, 0, 0.3); /* 3D shadow effect */
            word-wrap: break-word;
            text-overflow: ellipsis;
            overflow: hidden;
            white-space: nowrap;
            transition: all 0.3s ease-in-out;
        }

        h1:hover {
            color: #ff5722;
            text-shadow: 8px 8px 20px rgba(0, 0, 0, 0.5), -8px -8px 20px rgba(0, 0, 0, 0.5); /* Enhanced shadow on hover */
        }

        .description {
            font-size: 20px;
            margin-top: 20px;
            color: #555;
            max-width: 80%;
            text-align: center;
            margin: 20px auto;
        }

        .description p {
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2); /* Adding subtle shadow for text */
        }

    </style>
</head>
<body>

    <div class="container">
        <h1>3D Text Effects in CSS</h1>
        <div class="description">
            <p>This is an example of creating 3D text with effects like text-shadow, word-wrap, and text-overflow. Hover over the heading to see the effect!</p>
        </div>
    </div>

</body>
</html>
